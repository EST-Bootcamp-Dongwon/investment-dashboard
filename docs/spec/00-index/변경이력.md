# 변경 이력

> - **버전**: v2.0
> - **최종 수정**: 2026-08-07 (KST)
> - **역할**: ① 확정된 버전 이력 ② **아직 문서에 반영하지 않은 변경노트**
>
> [README 2.3절](../README.md#23-버전은-작업-중에-올리지-않는다) 규칙에 따라
> **작업 중에는 버전을 올리지 않습니다.** 스펙 변경은 아래 "미반영 변경노트" 에 쌓아두고,
> 마일스톤이 끝날 때 한 번에 반영하면서 버전을 올립니다.

---

## 1. 버전 이력

| 버전 | 날짜 (KST) | 폭 | 내용 |
| --- | --- | --- | --- |
| v2.0 | 2026-08-07 | Major | `docs/spec/` 신설. 00-index 4종 작성. 기능 ID F01~F29 / A01~A08 / X01~X05 최초 부여 |

---

## 2. 미반영 변경노트

다음 버전업 때 어느 문서에 반영할지까지 적어 둡니다.
`상태` 는 `미결`(결정 필요) / `대기`(결정됐고 반영만 남음) / `반영`(끝) 입니다.

### 결정이 필요한 것

<a id="cn-001"></a>
#### CN-001 · DB 엔진 미확정 — MongoDB Atlas ↔ Supabase(Postgres)

- **상태**: **미결** · **차단 중**: 세션 4 (`30-데이터/` 전체)
- **반영처**: `30-데이터/ERD.md` · `30-데이터/테이블-정의서.md` · `60-운영/배포-전략.md`

**경위.** 이전 세션에서 "MongoDB 재도입" 으로 잠정 결정했습니다. 근거는 강사님 원본의
`db.py`(motor 싱글턴)가 `projects/investment-analysis/app/backend/db.py` 에 보존돼 있다는
것이었습니다. 이후 사용자가 Supabase MCP 연동을 제시하면서 재검토가 필요해졌습니다.

**재검토에서 뒤집힌 전제.** 이 저장소에는 **MongoDB 코드가 한 줄도 없습니다.**
`grep -rniE "motor|pymongo|mongodb"` 결과는 `docker-compose.yml:6` 의 주석 한 줄뿐이고,
그 주석은 "강사님 원본에 있던 MongoDB 는 제거했다" 는 내용입니다.
즉 **"재도입" 의 비용 = "신규 도입" 의 비용** 이고, 이관 이점이 실제로는 없습니다.

**Supabase 무료 플랜 — 공식 문서 확인 (2026-08-07 조회)**

| 항목 | 값 | 출처 |
| --- | --- | --- |
| 활성 무료 프로젝트 | **2개** (Owner/Admin 기준 조직 합산). 일시정지 프로젝트는 쿼터에 미포함 | [Billing FAQ](https://supabase.com/docs/guides/platform/billing-faq) |
| DB 용량 | **500 MB / 프로젝트** | [Billing](https://supabase.com/docs/guides/platform/billing-on-supabase) |
| Storage | 1 GB | 〃 |
| Egress | 5 GB | 〃 |
| 인증 MAU | 50,000 | 〃 |
| Edge Function 호출 | 500,000 | 〃 |
| **자동 일시정지** | **7일간 활동이 적으면 정지.** 정지 후 **90일 이내** 대시보드에서 Resume. 하루 몇 건의 DB 요청이면 회피됨 | [Project Pausing](https://supabase.com/docs/guides/platform/free-project-pausing) |
| 백업 다운로드 | **무료 플랜은 불가** | [Production Checklist](https://supabase.com/docs/guides/deployment/going-into-prod) |
| pgvector | GA. `create extension vector` | [pgvector](https://supabase.com/docs/guides/database/extensions/pgvector) |

**계정 실측 (2026-08-07, Supabase MCP `list_projects`)**

| 프로젝트 | 리전 | 상태 |
| --- | --- | --- |
| america-stock-lecture | ap-northeast-2 | **ACTIVE_HEALTHY** |
| PLAF-Homepage | ap-northeast-2 | INACTIVE |
| stock-agent-dongwon | ap-northeast-2 | INACTIVE |
| AI-diary | ap-northeast-2 | INACTIVE |

→ 활성 1 / 정지 3. 무료 활성 슬롯이 **1개 남아 있습니다.** 리전은 전부 **서울**.

**판단 — Supabase 권고.**

1. 무료 슬롯이 실제로 남아 있고 리전이 서울이다 (지연 유리)
2. Postgres 라 **ERD 가 관계형으로 명확히 나온다.** SI 수준 심사(R-08)에 직접 유리
3. **인증이 무료로 딸려 온다** (50k MAU). R-08 의 "인증·권한 ❌" 를 코드 거의 없이 해소
4. **pgvector 로 Qdrant 를 대체**할 수 있다 → 외부 서비스 1개 감소. F27(RAG) 이 이미 있음
5. Storage 1GB → 차트 PNG·백테스트 리포트 HTML 보관처가 생김
6. MCP 가 연동돼 있어 **ERD 문서와 실제 마이그레이션을 같은 세션에서 대조**할 수 있다

**따라오는 제약 (설계에 반드시 반영)**

| 제약 | 대응 |
| --- | --- |
| DB 500MB | 시세 원본을 **넣지 않는다.** pykrx·yfinance 실시간 조회를 유지하고, DB 에는 사용자·포트폴리오·백테스트 **결과 메타와 요약만** 저장 |
| 7일 무활동 시 정지 | 데모·심사 전 Resume 확인. 또는 하루 1회 가벼운 쿼리로 keep-alive |
| 무료는 백업 다운로드 불가 | 스키마를 **마이그레이션 SQL 로 git 에 보관** (로컬이 정본) |
| 활성 슬롯 1개뿐 | 팀 프로젝트에서 또 필요하면 별도 조직을 만든다 |

> **사용자 결정 대기.** 이 판단대로 갈지, MongoDB Atlas 를 유지할지 확정해 주세요.

<a id="cn-003"></a>
#### CN-003 · SI 수준 요구(R-08)의 합격선

- **상태**: **미결**
- **반영처**: `10-기획/범위와-비범위.md`

인증까지인가, 배포까지인가, 자동화 테스트까지인가. 범위 문서를 쓰려면 선이 필요합니다.
→ [요구사항 대조표 Q-03](강사님-요구사항-대조표.md#5-남은-확인-사항)

---

### 결정은 끝났고 반영만 남은 것

<a id="cn-002"></a>
#### CN-002 · LEAN 컨테이너 용량 — **전제가 틀렸습니다**

- **상태**: 대기
- **반영처**: `60-운영/LEAN-백테스트-운영.md`

**제기된 우려.** "42.5GB 짜리 lean 모듈을 전략마다 일일이 받으면 디스크가 부족해진다."

**실측 결과 (2026-08-07).** 그렇지 않습니다.

```
$ docker system df
TYPE      TOTAL  ACTIVE  SIZE      RECLAIMABLE
Images    14     4       62.23GB   43.88GB (70%)

$ docker images | grep -i lean
hyundai-lean-module:local   42.5GB
samsung-lean-module:local   42.5GB
quantconnect/lean:latest    42.5GB
```

`docker images` 의 SIZE 는 **가상 크기**(모든 레이어 합)라서 공유 베이스를 중복 계산합니다.
실제 디스크는 `docker system df` 의 **62.23GB** 이지, 42.5×3 = 127.5GB 가 아닙니다.

이유는 두 파생 이미지가 모두 `FROM quantconnect/lean:latest` 이고
(`lean-hyundai/Dockerfile:1`, `lean-samsung/Dockerfile:1`),
그 위에 **`.py`·`.json`·`.sh` 7개를 COPY 할 뿐**이기 때문입니다.
전략 하나가 늘어도 실제 증가분은 수백 KB 입니다.

**따라서 42.5GB 는 전략 수와 무관하게 딱 한 번 지불합니다.**

**그래도 개선할 것 3가지**

| # | 조치 | 효과 |
| --- | --- | --- |
| 1 | COPY 대신 **볼륨 마운트**로 `/module` 주입 → 파생 이미지를 아예 만들지 않는다 | `--build` 불필요. 전략 추가 = 폴더 추가. `quantconnect/lean` 1벌만 유지 |
| 2 | `docker builder prune` — 빌드 캐시 **2.818GB** | 즉시 회수 |
| 3 | 백테스트 후 미사용 이미지 정리 루틴 (`docker image prune`) | 현재 회수 가능 **43.88GB** |

> 조치 1은 `docker-compose.hd.yaml` 주석이 경고하는
> "`--build` 를 안 붙이면 예전 코드가 그대로 돈다" 문제도 함께 없앱니다.
> **웹앱 요청 안에서 LEAN 컨테이너를 띄우지 않는 현재 설계는 유지합니다**
> (`backtest_lab.py` 는 벡터화 근사로 계산하고, 정본이 필요할 때만 엔진을 돌림).

**LEAN CLI 는 대안이 아닙니다.** `lean init` 이 QuantConnect 계정 로그인을 요구합니다
(엔진 자체는 인증하지 않는데 CLI 의 init 단계가 강제). 무인 실행에 부적합합니다.

<a id="cn-004"></a>
#### CN-004 · README 3절의 홈 화면 API 목록이 틀림

- **상태**: 대기 · **반영처**: `README.md`(저장소 루트) · `40-API/API-목록.md`

`home.js` 가 실제로 부르는 것은 두 개뿐입니다.
`GET /api/home/market-candle` (`home.js:130`) 과 `POST /api/market/snapshot` (`home.js:204`).

<a id="cn-005"></a>
#### CN-005 · `openapi_docs.py` 화이트리스트 누락 3건

- **상태**: 대기 · **반영처**: `app/backend/openapi_docs.py` · `40-API/API-목록.md`

누락: `/api/market/portfolio-combination` · `/api/quant/portfolio-scenario` · `/api/backtest-lab/*`
**셋 다 A/B 등급 기능** (F04·F05·F28) 이라 문서에서 빠지면 안 됩니다.

<a id="cn-006"></a>
#### CN-006 · `api.js` 바인딩 다수가 미사용

- **상태**: 대기 · **반영처**: `40-API/API-목록.md`

`api.<name>(` 형태의 정적 호출 기준으로 `views/`·`pages/` 어디에도 참조가 없는 항목:
`health` · `visitorHeartbeat` · `crossValidation` · `decisionBoundary` · `randomForest` ·
`kmeans` · `svm` · `mlp` · `linearRegression` · `opencv` · `huggingface` ·
`financialKnowledge` · `dartFinancialAnalysis` · `taxSample` · `taxSimulate`

> 이 방법은 **구조분해 호출을 못 잡습니다.** `taxSample`·`taxSimulate`·`dartFinancialAnalysis`
> 는 해당 view 가 `fetch('/api/...')` 로 직접 부르는 것을 확인했으므로 **오탐**입니다.
> 40-API 세션에서 하나씩 재확인합니다. 그중 `financialKnowledge` 는 진짜 미사용으로 보입니다
> (F24 가 프론트 전용인데 `POST /api/quant/financial-knowledge` 는 살아 있음).

<a id="cn-007"></a>
#### CN-007 · 라우트 48 → 51, 기능 ID F28 신설

- **상태**: 반영 (이번 세션) · **반영처**: `기능ID-대장.md`

커밋 `718f161` 로 `routers/backtest_lab.py` (라우트 3개) 가 들어왔습니다.
이전 세션 실측(48)은 그 직전 값입니다.

<a id="cn-008"></a>
#### CN-008 · 개념 설명 모달 — **이미 원형이 있습니다**

- **상태**: 대기 · **반영처**: `50-UI/개념설명-모달-사양.md`

요구: "버튼을 눌러 어떤 분석 기법을 썼고 그게 무엇인지 비전공자도 이해할 모달".

현재 두 가지가 **따로** 존재합니다.

| 구현 | 위치 | 형태 | 커버리지 |
| --- | --- | --- | --- |
| `PRACTICE_GUIDES` | `app.js:94` | 화면 상단 **정적 배너**. `technique` + 3~4줄 설명 | 25개 화면 |
| 몬테카를로 모달 | `views/portfolioSimulation.js:28` | **풀 모달**. 이름 유래 → 3단계 → 백분위 읽는 법 → **실제 `quant.py` 코드 스니펫** → 주의 문구 | **1개 화면** |

몬테카를로 모달이 요구를 정확히 충족합니다. **문제는 그게 1개 화면에만 있고 하드코딩이라는 것.**

**설계 방향**: 이 모달을 **데이터 주도 공통 컴포넌트**로 일반화합니다.
화면당 `{ 기법명, 원어·유래, 단계, 읽는 법, 코드 스니펫, 한계·주의 }` 를 넘기면 렌더되게 하고,
단계 설명에는 **mermaid 플로우차트**를 넣습니다. `PRACTICE_GUIDES` 25개는
그 데이터의 초안(요약 문장)으로 승격시켜 재활용합니다.

<a id="cn-009"></a>
#### CN-009 · 수업 단원을 기능 안으로 흡수

- **상태**: 대기 · **반영처**: `20-기능명세/*` · `50-UI/개념설명-모달-사양.md`

`02-preprocessing` · `03-vizualization` · `05-time-series` · `06-data-crawling` 을
기능에 녹입니다. 특히 **X01~X03(딥러닝 시계열) 폐기분을 `05-time-series` 의
통계적 시계열(분해·ARIMA·계절성)로 대체**합니다 —
`torch` 없이 되고, 개념 모달로 설명하기도 훨씬 쉽습니다.
→ [요구사항 대조표 4절](강사님-요구사항-대조표.md#4-수업-내용-흡수-계획-r-08-보강)

<a id="cn-010"></a>
#### CN-010 · 면책 문구가 29개 화면 중 5개에만 있음

- **상태**: 대기 · **반영처**: `50-UI/화면-상세.md`

R-07 미충족 원인. 공통 컴포넌트로 빼서 전 화면에 적용하면 **가장 싸게** 판정이 올라갑니다.

<a id="cn-011"></a>
#### CN-011 · 프론트 전용 기능이 6개 — A등급 F03 포함

- **상태**: 대기 · **반영처**: `20-기능명세/02-포트폴리오.md` · `60-운영/아키텍처.md`

F03(포트폴리오 추천) · F12(기술적 분석) · F18(재무제표 분석) · F19(밸류에이션) ·
F24(금융상품·자산배분) · F25(투자 성향) 가 백엔드를 부르지 않습니다.

**F03 이 문제입니다.** R-01 에 직결된 A등급 기능인데 추천 로직이 브라우저 안에만 있어
저장·재현·검증이 안 됩니다. CN-001(DB) 이 풀리면 함께 해결됩니다.

<a id="cn-012"></a>
#### CN-012 · F29(유튜브 자료실) 이 어디서도 링크되지 않음

- **상태**: 대기 · **반영처**: `50-UI/화면목록과-IA.md`

`pages/youtube.html` 로 가는 링크가 `index.html` 에도 `sidebar-nav.html` 에도 없습니다.
(비교: `pages/backtest-lab.html` 은 `index.html:130` 과 `sidebar-nav.html:34` 양쪽에 있음)
연결하거나 지웁니다.

<a id="cn-013"></a>
#### CN-013 · `app/backend/services/` 가 빈 껍데기

- **상태**: 대기 · **반영처**: `60-운영/아키텍처.md`

`__init__.py` (31 B) 만 있습니다. D-02 의 3단계 분해가 착지할 자리입니다.

---

## 3. 반영 요약

| 상태 | 개수 | ID |
| --- | --- | --- |
| **미결** (결정 필요) | 2 | CN-001 · CN-003 |
| 대기 (반영만 남음) | 10 | CN-002 · CN-004 ~ CN-006 · CN-008 ~ CN-013 |
| 반영 완료 | 1 | CN-007 |
