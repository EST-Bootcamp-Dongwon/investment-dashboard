# 강사님 요구사항 대조표

> - **버전**: v2.0
> - **최종 수정**: 2026-08-09 (KST)
> - **상태**: 초안 — **R-01~R-07 문항 원문 확정 대기** (아래 0절 · Q-01)
> - **기준 코드**: `main` @ `718f161`
> - **역할**: 강사님이 요구한 것 ↔ 지금 되는 것 ↔ 어느 문서가 책임지는가 를 한 장에.

---

## 0. 이 표의 출처 — 먼저 읽어 주세요

**R-01 ~ R-07** 은 저장소 안의 강사님 자료
[`docs/07.md`](../../07.md) 의 **"AI를 활용한 사용자 중심 포트폴리오 웹앱"** 절
(137~200행) 에서 도출했습니다. 구체적으로:

- **R-01 ~ R-04** ← "사용자가 보는 화면 예시" 표의 4개 행 (원문 그대로)
- **R-05** ← "쉬운 시뮬레이션을 만드는 방법" 절
- **R-06** ← "AI 웹앱을 테스트하는 방법" 절의 4단계
- **R-07** ← 같은 절 "4. 안전성 테스트"

**R-08** 은 2026-08-07 사용자가 전달한 추가 요구입니다
(팀 프로젝트를 SI 수준으로 하므로 개인 미니 프로젝트도 실질적으로 SI 수준일 것).

> ⚠ **확인 필요**: "필수 7항목" 이라는 표현은 이전 세션의 정리에서 나온 것이고,
> 강사님이 문서로 배포한 항목 번호가 따로 있다면 그 원문에 맞춰야 합니다.
> 원문(노션·PDF·구두 지시)이 있으면 알려 주세요. **번호와 문구를 그것에 맞춰 교체합니다.**
> 현재 표는 `docs/07.md` 만을 근거로 하며, 도출 구간을 위에 명시했습니다.

### 판정 기호

| 기호 | 뜻 |
| --- | --- |
| ✅ | 충족. 코드로 확인함 |
| 🔶 | 부분 충족. 되긴 하는데 요구를 다 못 채움 |
| ❌ | 미충족 |

---

## 1. 대조표

| ID | 요구 | 판정 | 지금 상태 (코드 근거) | 기능 ID | 담당 문서 |
| --- | --- | :---: | --- | --- | --- |
| **R-01** | **포트폴리오 추천** — 목표·기간·가격 하락 시 반응을 선택하면 안정/균형/성장 중심 구성과 **자산별 역할**을 보여준다 | 🔶 | 화면과 문구는 있으나 **백엔드 호출이 전혀 없다**. 추천 로직이 브라우저 안에만 있어 저장·재현·검증이 불가능 (`views/portfolioGuide.js`) | F03 | `20-기능명세/02-포트폴리오.md`<br>`40-API` |
| **R-02** | **포트폴리오 조합** — 두 종목을 골라 **신호등**과 흐름 차트로 동조성을 설명 | ✅ | 신호등 UI 구현됨 (`views/portfolioCombination.js` 내 8곳). `POST /api/market/portfolio-combination` 동작 | F04 | `20-기능명세/02-포트폴리오.md` |
| **R-03** | **포트폴리오 시뮬레이션** — 초기금액·월 적립액·기간 입력 → 보수적·중간·긍정적 **결과 범위** | ✅ | 5,000회 경로, 10/50/90 백분위를 보수·중간·긍정으로 제시 (`views/portfolioSimulation.js`, `POST /api/quant/portfolio-scenario`) | F05 | `20-기능명세/02-포트폴리오.md` |
| **R-04** | **AI 투자 도우미** — 모르는 용어·결과를 질문하면 일상어로 바꾸고 **확인할 질문을 제안** | 🔶 | RAG 챗 동작 (`POST /api/rag/ask`). **"확인할 질문 제안" 은 프롬프트에 없음이 확정됨** — 오히려 `rag.py:129~133` 이 원문 밖 문장을 금지 ([CN-014](변경이력.md#cn-014)). 또한 임베딩이 **해시 기반이라 의미 검색이 아님** ([CN-015](변경이력.md#cn-015)). `POST /api/rag/search` 는 미호출 | F27 | `20-기능명세/06-학습과-AI.md` |
| **R-05** | 몬테카를로를 **'정답 예측'이 아니라 '가능한 범위'** 로 분명히 표시 | ✅ | 전용 설명 모달에 "확정된 미래가 아니라 가능한 범위", "한 가지 예측 대신 수천 번의 서로 다른 시장 경로" 명시. 실제 코드 스니펫까지 노출 (`views/portfolioSimulation.js:28`) | F05 | `50-UI/개념설명-모달-사양.md` |
| **R-06** | **4단계 테스트** — ①이해도 ②시나리오 ③데이터·계산 ④안전성 | ❌ | `test/` 에 실습용 `.py` 6개뿐. 4단계 테스트 계획·수행 기록이 **전혀 없다** | 전체 | `60-운영/테스트-계획.md` |
| **R-07** | **안전성 문구** — 확정적 표현 금지, 교육용이며 개인 투자조언이 아님을 유지 | 🔶 | **강한 면책은 3개 파일뿐** (`portfolioGuide` · `portfolioSimulation` · `technicalChart`). 약한 주의까지 합쳐도 9개. **요구가 지목한 `ragChat`(F27) 에는 한 줄도 없음** — 측정 기준과 정정 경위는 [CN-016](변경이력.md#cn-016) | 전체 | `50-UI/화면-상세.md` |
| **R-08** | **SI 수준** — 말만 미니 프로젝트이고 기능은 실무 SI 프로젝트 수준일 것 (2026-08-07 추가) | ❌ | 아래 1.1 참고. **합격선은 2026-08-09 확정** → [범위와-비범위 4절](../10-기획/범위와-비범위.md#4-si-수준의-합격선--cn-003-결정) | 전체 | `60-운영/아키텍처.md` |

### 1.1 R-08 세부 — SI 수준 판정

SI 수준을 "실무 인수인계가 가능한 상태" 로 해석해 8개 축으로 나눴습니다.

| 축 | 판정 | 근거 |
| --- | :---: | --- |
| 계층 분리 (Controller→Service→Repository) | ❌ | `main.py` 단일 파일 **115,272 B** 에 라우트 22개. `app/backend/services/` 는 `__init__.py` 만 있는 **빈 껍데기** |
| 영속 저장 (DB) | ❌ | DB 가 **없다**. 모든 결과가 요청-응답 1회로 사라짐. 원본에 있던 MongoDB 는 제거됨 (`docker-compose.yml:6` 주석) |
| 인증·권한 | ❌ | 로그인·세션·JWT 코드가 **하나도 없다** (`grep` 실측) |
| DTO·입력 검증 | 🔶 | `pydantic` `BaseModel` 을 쓰는 라우터가 있으나 (`backtest_lab.py`) 전면 적용은 아님 |
| API 문서화 | 🔶 | `openapi_docs.py` 화이트리스트 방식. **3개 누락** (`/api/market/portfolio-combination` · `/api/quant/portfolio-scenario` · `/api/backtest-lab/*`) |
| 예외 처리·에러 응답 | 🔶 | `HTTPException` 사용처가 있으나 공통 에러 포맷이 없음 |
| 테스트 | ❌ | 자동화 테스트 없음 (R-06 과 같은 원인) |
| 배포·운영 | 🔶 | Docker Compose 4벌 존재. **배포 대상은 2026-08-09 Vercel 로 확정** ([CN-019](변경이력.md#cn-019)) 이나 아직 배포되지 않음 |

> **R-08 이 R-01 을 끌어내립니다.** R-01 이 🔶 인 이유(프론트 전용)와
> R-08 의 "영속 저장 없음" 은 같은 문제입니다. DB 를 붙이면 두 개가 함께 올라갑니다.

---

## 2. 요약

| 판정 | 개수 | ID |
| --- | --- | --- |
| ✅ 충족 | 3 | R-02 · R-03 · R-05 |
| 🔶 부분 | 3 | R-01 · R-04 · R-07 |
| ❌ 미충족 | 2 | R-06 · R-08 |

**충족 3 / 부분 3 / 미충족 2** (총 8항목)

### 2.1 무엇부터 손대야 하나

의존 관계를 따라가면 순서가 나옵니다.

```
DB 엔진 확정 (D-05)  ✅ 2026-08-09 Supabase
  └─> 영속 저장 (R-08)
        ├─> R-01 포트폴리오 추천 백엔드화  ← A등급 기능이 프론트 전용인 문제
        └─> 인증·권한 (R-08)
계층 분리 (D-02)
  └─> R-08 의 나머지 축 (DTO·에러·문서화)
테스트 계획 수립
  └─> R-06 (4단계 테스트)  ← 계획만 세워도 ❌ 에서 🔶 로 올라간다
면책 문구 공통 컴포넌트화
  └─> R-07 (5/29 → 29/29)  ← 가장 싸게 올릴 수 있는 항목
```

| 순위 | 할 일 | 올라가는 항목 | 비용 |
| --- | --- | --- | --- |
| 1 | ~~DB 엔진 확정~~ ✅ → **영속 저장 구현** | R-01 · R-08 | 큼 |
| 2 | `main.py` 3단계 분해 | R-08 | 큼 |
| 3 | 면책 문구를 공통 컴포넌트로 전 화면 적용 (**F27 부터**) | R-07 | **작음** |
| 4 | 4단계 테스트 계획 문서화 + ③④ 스크립트화 | R-06 | 중간 |
| 5 | RAG 프롬프트에 "확인할 질문 제안" 명시 | R-04 | **작음** |
| 6 | `openapi_docs.py` 화이트리스트 3건 보강 | R-08 | **작음** |
| 7 | RAG 임베딩을 의미 기반으로 교체 ([CN-015](변경이력.md#cn-015)) | R-04 | 중간 |
| 8 | 링크 없는 라우트 32개 정리 ([CN-018](변경이력.md#cn-018)) | R-08 · 심사 인상 | 중간 |

> 3·5·6 은 각각 한 시간 안쪽입니다. 먼저 처리해도 좋습니다.
> 특히 **3번의 첫 대상은 `ragChat.js`** 입니다 — R-07 이 명시적으로 지목한 화면인데 비어 있습니다.

---

## 3. 요구를 넘어서는 부분 — 감점이 아니라 가점 요소

강사님 요구에는 없지만 이미 있거나 계획 중인 것들입니다.
[기능ID-대장](기능ID-대장.md) 의 나머지 25개 기능이 여기 해당합니다.

| 영역 | 내용 | 기능 ID |
| --- | --- | --- |
| 공시 데이터 | DART 4종 (기업검색·지역조회·계열사 네트워크·재무 AI 분석) | F20~F23 |
| 산업 분석 | Porter 5 Forces · 섹터·피어·생애주기 | F16 |
| 거시경제 | 실시간 지표 · GBM 시뮬레이션 · KOSPI 제외 지수 | F13~F15 |
| 퀀트 | MPT 최적화 · VaR · 백테스트 · 파이프라인 | F06~F09 |
| **백테스트 실험실** | LEAN 엔진 기반 워크포워드 검증 (2025 학습 → 2026 예측) | F28 |
| 세무·회계 | 엑셀 업로드 → 세액 시뮬레이션 | F26 |

> **F28(LEAN 백테스트)은 강사님 요구를 크게 넘어서는 항목입니다.**
> 다만 컨테이너가 42.5GB 라 운영 정책이 필요합니다 → `60-운영/LEAN-백테스트-운영.md`,
> [CN-002](변경이력.md#cn-002)

---

## 4. 수업 내용 흡수 계획 (R-08 보강)

"SI 수준" 을 채우는 동시에 수업 복습이 되도록, 배운 단원을 기능 안으로 흡수합니다.
상세 설계는 `50-UI/개념설명-모달-사양.md` 와 `20-기능명세` 에서 다룹니다.

| 수업 단원 | 흡수처 | 흡수 방식 |
| --- | --- | --- |
| `02-preprocessing` (판다스 전처리) | F17 · F18 · F26 | 결측·이상치 처리 과정을 화면에 노출 |
| `03-vizualization` (시각화) | 전 화면 | 차트 유형 선택 근거를 개념 모달에 기록 |
| `05-time-series` (분해·ARIMA·계절성) | F05 · F28 | **X01~X03(딥러닝) 폐기분을 통계적 시계열로 대체** |
| `06-data-crawling` (정적·동적 크롤링·OpenAPI) | F20~F23 · F29 | DART·KRX·공공데이터포털 수집 계층으로 정리 |

---

## 5. 남은 확인 사항

| # | 확인할 것 | 누가 | 상태 |
| --- | --- | --- | --- |
| Q-01 | "필수 7항목" 의 **원문**이 따로 있는가 (노션·PDF·구두)? 있다면 이 표를 그것에 맞춘다 | 사용자 | **미해소** |
| Q-02 | R-04 "확인할 질문 제안" 이 RAG 프롬프트에 실제로 들어 있는가 | 코드 확인 | ✅ **없음** (2026-08-09) → [CN-014](변경이력.md#cn-014) |
| Q-03 | R-08 "SI 수준" 의 합격선을 어디에 둘 것인가 (인증까지? 배포까지?) | 사용자 | ✅ **확정** (2026-08-09) → [CN-003](변경이력.md#cn-003) |
| Q-04 | DART·KOSIS·BOK 의 **실제 호출 한도**와 운영계정 필요 여부 | 사용자·조사 | ⚠ **부분 해소** (2026-08-09) → [`30-데이터/외부-데이터소스.md`](../30-데이터/외부-데이터소스.md) |
| Q-05 | 벡터 저장소를 pgvector 로 옮길지, Qdrant Cloud 를 유지할지 | 세션 4 | ✅ **pgvector 확정** (2026-08-09) → [CN-021](변경이력.md#cn-021) |

> **Q-03 요약**: 인증·배포까지는 **넣고**, CI/CD 파이프라인과 테스트의 CI 자동 실행은 **뺍니다.**
> 축별 합격 조건은 [범위와-비범위 4절](../10-기획/범위와-비범위.md#4-si-수준의-합격선--cn-003-결정)에 있습니다.
>
> **Q-04 요약** (2026-08-09 조사):
> - **DART ✅** — 일 **20,000건**. 응답 코드 `020` = "요청 제한을 초과하였습니다.
>   일반적으로는 20,000건 이상의 요청"
>   ([개발가이드](https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS001&apiId=2019018)).
>   덤으로 `021` = **한 번에 조회 가능한 회사 최대 100건**.
> - **KOSIS · BOK ⚠** — 한도 수치는 공식 페이지가 SPA 라 **확인 실패**.
>   다만 **두 키 모두 코드가 전혀 쓰지 않는다**는 사실을 확인했습니다
>   (`docker-compose.yml:25~26` 선언만, 코드 참조 0건) → [CN-024](변경이력.md#cn-024).
>   **운영계정 필요 여부는 그 결정 이후에 판단합니다.**
> - **새로 드러난 위험** — DART 는 한도보다 **우리 코드가 더 문제**입니다.
>   `_load_all_listed_details()` 가 한 번에 수천 건을 소모합니다 → [CN-023](변경이력.md#cn-023).
