# 테이블 정의서

> - **버전**: v2.0
> - **최종 수정**: 2026-08-09 (KST)
> - **상태**: 초안
> - **기준 코드**: `main` @ `c3d737e`
> - **DB**: Supabase(Postgres) · pgvector — [CN-001](../00-index/변경이력.md#cn-001) · [CN-019③](../00-index/변경이력.md#cn-019)
> - **역할**: 컬럼·타입·제약·인덱스의 정본. 관계와 설계 근거는
>   [`ERD.md`](ERD.md) 에 있다.

---

## 1. 명명 규칙

| 대상 | 규칙 | 예 |
| --- | --- | --- |
| 테이블 | `snake_case` **단수형** | `recommendation`, `app_user` |
| 컬럼 | `snake_case` | `initial_amount` |
| 기본키 | `id` | — |
| 외래키 | `<참조테이블>_id` | `recommendation_id` |
| 시각 컬럼 | `_at` 접미사 · **`timestamptz`** | `created_at` |
| 인덱스 | `idx_<테이블>_<컬럼들>` | `idx_recommendation_user_created` |
| 제약 | `ck_` / `uq_` / `fk_` 접두 | `ck_recommendation_item_weight` |

> **`user` 를 테이블 이름으로 쓰지 않습니다.** Postgres 예약어입니다. `app_user` 를 씁니다.

**시각은 전부 `timestamptz`(UTC 저장) 입니다.** 표시 단계에서 KST 로 변환합니다.
`timestamp`(타임존 없음)를 쓰면 배포 환경과 로컬의 기준이 갈라집니다.

---

## 2. 공용 타입

```sql
-- 투자 성향 프로필. F03(추천 결과)과 F05(시뮬레이션 입력)가 같은 어휘를 씁니다.
--   F03: app/frontend/js/views/portfolioGuide.js:2,13,25
--   F05: app/backend/routers/quant.py:67~69, 검증 패턴 quant.py:51
create type portfolio_profile as enum ('stable', 'balanced', 'growth');

-- F03 설문 3문항. 값은 portfolioGuide.js:95~97 의 <option value> 실측입니다.
create type invest_goal    as enum ('growth', 'balance', 'protect');
create type invest_horizon as enum ('short', 'medium', 'long');
create type risk_appetite  as enum ('low', 'medium', 'high');

-- F04 비교 기간. 검증 패턴 app/backend/main.py:1106
create type combination_period as enum ('3mo', '6mo', '1y', '2y');
```

> **enum 과 CHECK 중 enum 을 골랐습니다.** 이 5개는 프론트 `<option>`·백엔드
> `pattern=` 양쪽에 이미 고정돼 있어 값이 늘어날 일이 드뭅니다. 대신 값을 추가하려면
> `ALTER TYPE … ADD VALUE` 가 필요하다는 대가를 집니다. 값이 자주 바뀌는 축이라면
> CHECK 가 나았을 겁니다.

**주의 — `invest_goal` 의 `balance` 는 오타가 아닙니다.**
`portfolioGuide.js:95` 의 실제 값이 `balance` 이고,
프로필 쪽(`portfolio_profile`)은 `balanced` 입니다. **두 어휘가 실제로 다릅니다.**
통일하고 싶다면 프론트 `<option value>` 를 먼저 고쳐야 하며, 그건 스펙 변경이라
[CN-022](../00-index/변경이력.md#cn-022) 로 올려 두었습니다.

---

## 3. 확장

```sql
create extension if not exists vector with schema extensions;
```

출처: [Supabase — Vector columns](https://supabase.com/docs/guides/ai/vector-columns) (2026-08-09 확인).

---

## 4. 테이블

### 4.1 `app_user` — 사용자 프로필

Supabase Auth 의 `auth.users` 를 **대체하지 않고 확장**합니다.
이메일·비밀번호·세션은 전부 `auth.users` 가 관리합니다.

| 컬럼 | 타입 | 제약 | 설명 |
| --- | --- | --- | --- |
| `id` | `uuid` | PK, FK → `auth.users(id)` ON DELETE CASCADE | Auth 사용자와 1:1 |
| `display_name` | `text` | NULL, `length ≤ 40` | 화면 표시명 |
| `default_profile` | `portfolio_profile` | NULL | 마지막 추천 결과를 기본값으로 기억 |
| `created_at` | `timestamptz` | NOT NULL, `default now()` | |
| `updated_at` | `timestamptz` | NOT NULL, `default now()` | 트리거로 갱신 |

> **이메일을 복사해 두지 않습니다.** `auth.users.email` 이 정본이고, 사본을 만들면
> 변경 시 어긋납니다. 필요하면 조인합니다.

---

### 4.2 `recommendation` — F03 포트폴리오 추천 결과

**F03 을 백엔드화하면서 생기는 테이블입니다** ([CN-011](../00-index/변경이력.md#cn-011) 해소).

| 컬럼 | 타입 | 제약 | 설명 |
| --- | --- | --- | --- |
| `id` | `bigint` | PK, `generated always as identity` | |
| `user_id` | `uuid` | NULL, FK → `app_user(id)` ON DELETE SET NULL | 비로그인이면 NULL |
| `anon_id` | `text` | NULL, `length ≤ 64` | 비로그인 브라우저 식별자 |
| `goal` | `invest_goal` | NOT NULL | 투자 목적 (`portfolioGuide.js:95`) |
| `horizon` | `invest_horizon` | NOT NULL | 투자 기간 (`portfolioGuide.js:96`) |
| `risk` | `risk_appetite` | NOT NULL | 하락 반응 (`portfolioGuide.js:97`) |
| `profile` | `portfolio_profile` | NOT NULL | 판정 결과 (`portfolioGuide.js:133`) |
| `profile_label` | `text` | NOT NULL, `length ≤ 40` | `'안정 중심 구성'` 등 (`portfolioGuide.js:3,14,26`) |
| `created_at` | `timestamptz` | NOT NULL, `default now()` | |

**제약**

```sql
-- 로그인이든 비로그인이든 소유자를 알 수 없는 행은 만들지 않는다.
constraint ck_recommendation_owner
  check (user_id is not null or anon_id is not null)
```

**인덱스**

| 인덱스 | 컬럼 | 용도 |
| --- | --- | --- |
| `idx_recommendation_user_created` | `(user_id, created_at desc)` | 로그인 사용자의 최근 이력 |
| `idx_recommendation_anon_created` | `(anon_id, created_at desc)` | 비로그인 이력 |

---

### 4.3 `recommendation_item` — 추천 자산 배분 라인

`PROFILES[*].items` (`portfolioGuide.js:5~10,17~23,29~35`) 를
**그 시점 값으로 스냅샷** 저장합니다. 근거는 [`ERD.md` 2.1절](ERD.md#21-f03-의-백엔드화가-이-erd-의-핵심).

| 컬럼 | 타입 | 제약 | 설명 |
| --- | --- | --- | --- |
| `id` | `bigint` | PK, identity | |
| `recommendation_id` | `bigint` | NOT NULL, FK → `recommendation(id)` ON DELETE CASCADE | |
| `sort_order` | `smallint` | NOT NULL, `≥ 0` | 화면 표시 순서 |
| `asset_name` | `text` | NOT NULL, `length ≤ 40` | `'글로벌 주식'` 등 |
| `weight_pct` | `smallint` | NOT NULL, `between 0 and 100` | 비중 |
| `explanation` | `text` | NOT NULL, `length ≤ 200` | `'장기 성장 기회를 위한 부분'` 등 |

**제약**

```sql
constraint uq_recommendation_item_order unique (recommendation_id, sort_order)
constraint ck_recommendation_item_weight check (weight_pct between 0 and 100)
```

> **색상(`#22c55e` 등)은 저장하지 않습니다.** `items` 배열의 3번째 원소이지만
> **UI 테마의 관심사**입니다. 다크모드나 색 팔레트를 바꾸면 과거 이력의 색까지
> 손봐야 하는 문제가 생깁니다. 색은 `asset_name` 기준으로 프론트에서 매핑합니다.

**비중 합계 100 은 어떻게 보장하나** — 행 단위 CHECK 로는 불가능합니다.

| 방법 | 판단 |
| --- | --- |
| `DEFERRABLE` 제약 + 트리거 | 정확하지만 이 규모에 과함 |
| **서버 계층에서 검증 후 한 트랜잭션 삽입** | **채택.** 배분표는 서버 상수에서 오므로 입력원이 하나 |
| 뷰로 사후 감시 | 병행. 아래 참조 |

```sql
-- 합계가 100이 아닌 추천을 찾아내는 점검용 뷰 (운영 중 사후 감시)
create view v_recommendation_weight_check as
select recommendation_id, sum(weight_pct) as total
from recommendation_item
group by recommendation_id
having sum(weight_pct) <> 100;
```

---

### 4.4 `combination_query` — F04 조합 조회 이력

응답 필드 출처: `app/backend/main.py:1234~1243`.

| 컬럼 | 타입 | 제약 | 설명 |
| --- | --- | --- | --- |
| `id` | `bigint` | PK, identity | |
| `user_id` | `uuid` | NULL, FK → `app_user(id)` ON DELETE SET NULL | |
| `anon_id` | `text` | NULL, `length ≤ 64` | |
| `ticker_a` | `text` | NOT NULL, `length between 1 and 20` | `main.py:1104` |
| `ticker_b` | `text` | NOT NULL, `length between 1 and 20` | `main.py:1105` |
| `period` | `combination_period` | NOT NULL | `main.py:1106` |
| `signal` | `text` | NOT NULL, `length ≤ 20` | 신호등 판정 |
| `summary` | `text` | NOT NULL, `length ≤ 500` | 한 줄 해설 |
| `portfolio_hint` | `text` | NULL, `length ≤ 500` | 분산 힌트 |
| `latest_data_at` | `timestamptz` | NULL | 조회한 시세의 마지막 시각 |
| `created_at` | `timestamptz` | NOT NULL, `default now()` | |

> **`chart_points` 는 저장하지 않습니다.** 시세 파생물이고 행 수가 큽니다 (제약 C-04).
> `(ticker_a, ticker_b, period)` 로 언제든 다시 계산됩니다.
>
> **`latest_data_at` 은 남깁니다.** 같은 입력이라도 시세가 갱신되면 결과가 달라지므로,
> "이 판정은 어느 시점 데이터였나" 가 증거로 필요합니다.

**인덱스**: `(user_id, created_at desc)` · `(anon_id, created_at desc)` ·
`(ticker_a, ticker_b)` — 인기 조합 통계용.

---

### 4.5 `simulation_run` / `simulation_point` — F05 시뮬레이션

#### `simulation_run`

요청 출처 `app/backend/routers/quant.py:50~55`, 응답 출처 `quant.py:93~101`.

| 컬럼 | 타입 | 제약 | 설명 |
| --- | --- | --- | --- |
| `id` | `bigint` | PK, identity | |
| `user_id` | `uuid` | NULL, FK ON DELETE SET NULL | |
| `anon_id` | `text` | NULL, `length ≤ 64` | |
| `profile` | `portfolio_profile` | NOT NULL | `quant.py:51` |
| `profile_label` | `text` | NOT NULL, `length ≤ 20` | `'균형 중심'` 등 `quant.py:67~69` |
| `initial_amount` | `bigint` | NOT NULL, `between 0 and 1000000000` | `quant.py:52` |
| `monthly_amount` | `bigint` | NOT NULL, `between 0 and 100000000` | `quant.py:53` |
| `years` | `smallint` | NOT NULL, `between 1 and 30` | `quant.py:54` |
| `total_paid` | `bigint` | NOT NULL, `≥ 0` | 원금 합계 `quant.py:91` |
| `final_cautious` | `bigint` | NOT NULL, `≥ 0` | 최종 p10 |
| `final_middle` | `bigint` | NOT NULL, `≥ 0` | 최종 p50 |
| `final_positive` | `bigint` | NOT NULL, `≥ 0` | 최종 p90 |
| `rng_seed` | `integer` | NOT NULL, `default 20260806` | `quant.py:72` |
| `paths` | `integer` | NOT NULL, `default 5000` | `quant.py:73` |
| `created_at` | `timestamptz` | NOT NULL, `default now()` | |

> **`rng_seed` 와 `paths` 를 컬럼으로 둔 이유.** 지금은 코드에 하드코딩된 상수지만,
> 이 두 값이 바뀌면 **같은 입력이 다른 결과를 냅니다.** 재현의 전제이므로
> 결과와 함께 박아 둡니다. 나중에 시드를 바꿔도 과거 이력을 재현할 수 있습니다.

**제약**

```sql
constraint ck_simulation_run_owner check (user_id is not null or anon_id is not null)
constraint ck_simulation_run_percentile check (final_cautious <= final_middle
                                           and final_middle  <= final_positive)
```

> 마지막 제약은 p10 ≤ p50 ≤ p90 이라는 **백분위의 정의**입니다.
> 계산 오류가 DB 에 들어오는 것을 막습니다.

#### `simulation_point`

| 컬럼 | 타입 | 제약 | 설명 |
| --- | --- | --- | --- |
| `id` | `bigint` | PK, identity | |
| `simulation_run_id` | `bigint` | NOT NULL, FK → `simulation_run(id)` ON DELETE CASCADE | |
| `year` | `smallint` | NOT NULL, `between 0 and 30` | 0 = 시작 시점 `quant.py:77` |
| `cautious` | `bigint` | NOT NULL, `≥ 0` | p10 |
| `middle` | `bigint` | NOT NULL, `≥ 0` | p50 |
| `positive` | `bigint` | NOT NULL, `≥ 0` | p90 |

**제약**: `unique (simulation_run_id, year)`

**행 수 예측**: 실행당 최대 31행 (`years ≤ 30` + 0년차).
1만 회 실행해도 31만 행 ≈ 수십 MB 이하로, 500MB 제약 안에서 안전합니다.

> **정규화 vs JSONB.** 최대 31행이라 JSONB 한 컬럼으로도 됩니다.
> 그럼에도 테이블로 나눈 것은 **ERD 의 관계 표현력이 SI 수준 심사(R-08)에
> 직접 유리**하기 때문입니다 ([CN-003](../00-index/변경이력.md#cn-003) 8축 중 "영속 저장").
> 순수 기술적으로는 JSONB 가 더 간단하다는 점을 인정합니다.

---

### 4.6 `backtest_summary` — F28 백테스트 요약

요청 출처 `app/backend/routers/backtest_lab.py:62~74`,
응답 출처 `backtest_lab.py:205~228`.

| 컬럼 | 타입 | 제약 | 설명 |
| --- | --- | --- | --- |
| `id` | `bigint` | PK, identity | |
| `user_id` | `uuid` | NULL, FK ON DELETE SET NULL | |
| `anon_id` | `text` | NULL, `length ≤ 64` | |
| `ticker` | `text` | NOT NULL, `length ≤ 24` | `backtest_lab.py:63` |
| `name` | `text` | NOT NULL, `length ≤ 40` | `backtest_lab.py:64` |
| `train_start` | `date` | NOT NULL | `backtest_lab.py:65` |
| `train_end` | `date` | NOT NULL | `backtest_lab.py:66` |
| `test_start` | `date` | NOT NULL | `backtest_lab.py:67` |
| `test_end` | `date` | NOT NULL | `backtest_lab.py:68` |
| `initial_cash` | `bigint` | NOT NULL, `between 1000000 and 1000000000000` | `backtest_lab.py:69` |
| `commission_rate` | `numeric(6,5)` | NOT NULL, `between 0 and 0.02` | `backtest_lab.py:70` |
| `sell_tax_rate` | `numeric(6,5)` | NOT NULL, `between 0 and 0.02` | `backtest_lab.py:71` |
| `slippage_rate` | `numeric(6,5)` | NOT NULL, `between 0 and 0.02` | `backtest_lab.py:72` |
| `warmup_days` | `smallint` | NOT NULL, `between 30 and 750` | `backtest_lab.py:74` |
| `model_name` | `text` | NOT NULL | `'sklearn HistGradientBoostingRegressor'` `backtest_lab.py:214` |
| `strategy_stats` | `jsonb` | NOT NULL | `BacktestStats` 12필드 |
| `benchmark_stats` | `jsonb` | NOT NULL | 〃 (매수 후 보유) |
| `prediction_metrics` | `jsonb` | NOT NULL | `PredictionMetrics` |
| `price_rows` | `integer` | NOT NULL, `≥ 0` | 조회한 시세 행 수 `backtest_lab.py:217` |
| `price_first` | `date` | NOT NULL | `backtest_lab.py:218` |
| `price_last` | `date` | NOT NULL | `backtest_lab.py:219` |
| `engine_note` | `text` | NOT NULL | `'간이(벡터화) 계산 — 정본은 LEAN…'` `backtest_lab.py:220` |
| `created_at` | `timestamptz` | NOT NULL, `default now()` | |

**`numeric` 을 쓴 이유** — 수수료·세율은 `0.00015` 같은 값이고, 성과 계산에서
누적 곱셈에 들어갑니다. `float` 의 이진 오차가 재현 검증을 방해할 수 있어
**정확한 10진 저장**을 씁니다. 반면 성과 지표(`sharpe` 등)는 어차피 근사값이라
JSONB 안의 숫자로 둡니다.

**`jsonb` 3개만 예외적으로 허용한 이유**

| 항목 | 필드 수 | 판단 |
| --- | --- | --- |
| `BacktestStats` (`lean-hyundai/hd_core.py:324~336`) | 12 | 지표 정의가 아직 바뀔 수 있음. 전략·벤치마크 2벌이라 컬럼화하면 24개 |
| `PredictionMetrics` (`hd_core.py:135~155`) | 15+ | 〃 |

컬럼으로 펼치면 **한 테이블에 40개 가까운 지표 컬럼**이 생기고, 지표를 하나 추가할
때마다 마이그레이션이 필요합니다. F28 은 B등급이고 실험 성격이므로 **JSONB 로 둡니다.**
대신 자주 정렬·필터할 지표는 **생성 컬럼**으로 꺼냅니다.

```sql
-- 자주 쓰는 지표만 인덱싱 가능한 형태로 승격
alter table backtest_summary
  add column strategy_total_return_pct numeric
    generated always as ((strategy_stats->>'total_return_pct')::numeric) stored,
  add column strategy_max_drawdown_pct numeric
    generated always as ((strategy_stats->>'max_drawdown_pct')::numeric) stored;
```

> **`rows`(일별 예측)·`curve`(일별 자산곡선)는 저장하지 않습니다.** 제약 C-04.
> 검증 구간이 6개월이면 행이 120개 남짓이고 실행마다 쌓이면 500MB 를 위협합니다.
> 입력 11개가 남아 있으므로 재실행으로 복원합니다 ([`ERD.md` 0.2절](ERD.md#02-입력을-저장하면-결과가-재현된다)).

---

### 4.7 `doc_chunk` — F27 RAG 벡터 (pgvector)

Qdrant 컬렉션 `investment_docs` (`rag.py:17`) 를 대체합니다.
payload 구조는 `scripts/upload_docs_to_qdrant.sh` 의 `records` 와 같습니다.

| 컬럼 | 타입 | 제약 | 설명 |
| --- | --- | --- | --- |
| `id` | `bigint` | PK, identity | |
| `source_doc` | `text` | NOT NULL | `'07.md'` 등 |
| `chunk_index` | `integer` | NOT NULL, `≥ 0` | 문서 내 순번 |
| `content` | `text` | NOT NULL | 청크 원문 (최대 1,200자) |
| `embedding` | `extensions.vector(768)` | NULL | Gemini `gemini-embedding-001` 절단·정규화 |
| `embedding_hash` | `extensions.vector(384)` | NULL | 폴백용 `_hash_embed` (`rag.py:52`) |
| `embed_method` | `text` | NOT NULL, `in ('gemini','hash')` | 어느 컬럼이 유효한지 |
| `token_count` | `integer` | NULL | 색인 시 실측 (2,048 상한 감시용) |
| `indexed_at` | `timestamptz` | NOT NULL, `default now()` | |

**제약**

```sql
constraint uq_doc_chunk unique (source_doc, chunk_index)
constraint ck_doc_chunk_embedding check (
  (embed_method = 'gemini' and embedding      is not null) or
  (embed_method = 'hash'   and embedding_hash is not null)
)
```

> **벡터 컬럼을 두 개로 나눈 이유.** 차원이 768 과 384 로 **다릅니다.**
> `vector(n)` 은 차원이 타입의 일부라 한 컬럼에 섞을 수 없습니다.
> 폴백이 필요한 근거는 [`ERD.md` 4.3절](ERD.md#43-임베딩--무료-조건에서의-선택).

**인덱스**

```sql
-- 코사인 거리 기준 근사 최근접 탐색
create index idx_doc_chunk_embedding on doc_chunk
  using hnsw (embedding extensions.vector_cosine_ops);
```

| 확인 사항 | 내용 |
| --- | --- |
| 인덱스 차원 한계 | pgvector 는 `vector` 타입 최대 16,000 차원, **인덱스는 2,000 차원까지** ([pgvector README](https://github.com/pgvector/pgvector), 2026-08-09 확인). 768 은 안전, **기본값 3072 는 인덱스 불가** |
| 거리 연산자 일치 | 색인·질의 모두 코사인(`<=>`). Qdrant 설정도 `"distance": "Cosine"` 이었습니다 (`upload_docs_to_qdrant.sh`) — 동작이 바뀌지 않습니다 |
| 이 규모에 인덱스가 필요한가 | **178행이면 순차 스캔으로 충분합니다.** 인덱스는 문서가 늘어날 때를 위한 것이고, 지금은 성능이 아니라 **설계 정합성** 목적입니다 |

**검색 함수** — 프론트에 SQL 을 노출하지 않기 위해 RPC 로 감쌉니다.

```sql
create or replace function match_doc_chunk(
  query_embedding extensions.vector(768),
  match_count int default 5,
  score_threshold float default 0.0
)
returns table (id bigint, source_doc text, chunk_index int, content text, score float)
language sql stable
as $$
  select c.id, c.source_doc, c.chunk_index, c.content,
         1 - (c.embedding <=> query_embedding) as score
  from doc_chunk c
  where c.embedding is not null
    and 1 - (c.embedding <=> query_embedding) >= score_threshold
  order by c.embedding <=> query_embedding
  limit match_count;
$$;
```

> 반환 필드는 현행 `rag.py:86~91` 의 `{score, source_doc, chunk_index, text}` 와
> 이름을 맞췄습니다(`text` → `content` 만 다름). **프론트 `ragChat.js` 의 변경을
> 최소화하기 위해서**이고, 응답 직렬화 단계에서 `content` → `text` 로 되돌립니다.

---

## 5. 마이그레이션 방침

### 5.1 왜 SQL 파일이 정본인가

Supabase 무료 플랜은 **백업 다운로드를 제공하지 않습니다**
([Production Checklist](https://supabase.com/docs/guides/deployment/going-into-prod),
[CN-001](../00-index/변경이력.md#cn-001) 기록). 프로젝트가 정지되거나 날아가면
대시보드에서 만든 스키마는 복구 수단이 없습니다.

**따라서 스키마의 정본은 git 안의 SQL 파일입니다.**
[전역 규칙 8.1](../../../CLAUDE.md) 의 "로컬이 정본" 과 같은 원칙입니다.

### 5.2 파일 배치와 이름

```
supabase/
  migrations/
    20260809120000_init_types.sql          -- 2절 enum + 3절 확장
    20260809120100_init_user.sql           -- app_user
    20260809120200_init_recommendation.sql -- recommendation, recommendation_item
    20260809120300_init_combination.sql    -- combination_query
    20260809120400_init_simulation.sql     -- simulation_run, simulation_point
    20260809120500_init_backtest.sql       -- backtest_summary
    20260809120600_init_doc_chunk.sql      -- doc_chunk + hnsw + match_doc_chunk()
    20260809120700_init_rls.sql            -- 6절 RLS 정책 일괄
  seed.sql                                 -- 로컬 개발용 표본 데이터
```

- 파일명은 **`YYYYMMDDHHmmss_설명.sql`** (Supabase CLI 규약). 시각은 **KST 기준으로 적되
  내용상 순서만 지키면 됩니다.**
- **한 파일 = 한 관심사.** 위 8개는 서로 독립적으로 읽힙니다.
- RLS 를 마지막 파일로 몬 것은 **모든 테이블이 존재한 뒤에 정책을 걸어야** 하기 때문입니다.

### 5.3 규칙

| 규칙 | 이유 |
| --- | --- |
| **적용한 마이그레이션은 수정하지 않는다.** 항상 새 파일을 추가한다 | 이미 적용된 파일을 고치면 로컬과 원격 스키마가 조용히 갈라집니다 |
| 되돌리기는 **역방향 마이그레이션 파일**로 한다 | 무료 플랜에 백업이 없어 "복원으로 되돌리기" 가 불가능합니다 |
| `drop table` 은 사용자 승인 후에만 쓴다 | 되돌릴 수 없습니다 |
| 대시보드 SQL Editor 에서 **직접 스키마를 바꾸지 않는다** | git 에 기록이 남지 않습니다. 불가피했다면 즉시 같은 내용의 마이그레이션 파일을 만들어 맞춥니다 |
| 적용은 Supabase CLI 또는 MCP `apply_migration` 으로 한다 | 적용 이력이 `list_migrations` 로 조회됩니다 |

### 5.4 아직 안 한 것

**이 문서는 설계입니다. 실제 Supabase 프로젝트에는 아직 아무것도 적용하지 않았습니다.**
계정 실측(2026-08-07)에서 확인한 활성 슬롯 1개는 그대로 비어 있습니다
([CN-001](../00-index/변경이력.md#cn-001)). 적용은 구현 단계에서 합니다.

---

## 6. RLS (행 수준 보안)

Supabase 는 **RLS 를 켜지 않으면 `anon` 키로 테이블 전체가 읽힙니다.**
공개 배포([CN-019](../00-index/변경이력.md#cn-019))가 전제이므로 필수입니다.

```sql
alter table app_user            enable row level security;
alter table recommendation      enable row level security;
alter table recommendation_item enable row level security;
alter table combination_query   enable row level security;
alter table simulation_run      enable row level security;
alter table simulation_point    enable row level security;
alter table backtest_summary    enable row level security;
alter table doc_chunk           enable row level security;
```

### 6.1 정책 요약

| 테이블 | SELECT | INSERT | UPDATE / DELETE |
| --- | --- | --- | --- |
| `app_user` | 본인만 (`auth.uid() = id`) | 본인만 | 본인만 |
| 이력 4종 | 본인 행만 (`auth.uid() = user_id`) | 인증 사용자: 본인 `user_id` 로만 | 본인만 |
| `recommendation_item` · `simulation_point` | 부모 행이 보이면 보임 | 서버 경유만 | 금지 |
| `doc_chunk` | **누구나 SELECT** | 서버 경유만 | 서버 경유만 |

```sql
-- 예: 추천 이력 조회
create policy "본인 추천만 조회"
  on recommendation for select
  using (auth.uid() = user_id);

-- 예: 문서 청크는 공개 지식이므로 읽기만 허용
create policy "문서 청크 공개 조회"
  on doc_chunk for select
  using (true);
```

### 6.2 비로그인 이력은 RLS 로 못 막습니다 — 정면으로 인정합니다

`anon_id` 는 브라우저가 보내는 값이라 **정책 조건으로 쓰면 아무나 남의 `anon_id` 를
넣어 조회할 수 있습니다.** RLS 로 해결되지 않습니다.

**대응**

1. 비로그인 이력은 **`anon` 키로 직접 조회하지 않습니다.** FastAPI 서버가
   **`service_role` 키로 대신 조회**하고, `anon_id` 는 요청 쿠키에서만 읽습니다.
2. 비로그인 이력에 **개인정보를 저장하지 않습니다** (티커·금액·기간뿐).
3. `service_role` 키는 **서버 환경변수로만** 둡니다. 프론트에 내려가면 RLS 가 전부 무력화됩니다.

> **남는 위험을 명시합니다.** 위 1번은 "서버를 믿는다" 는 전제입니다.
> 비로그인 이력의 기밀성은 **보장하지 않으며**, 그 사실을 화면에도 적습니다
> ([`../50-UI/화면-상세.md`](../50-UI/화면-상세.md) 에서 다룸 — 아직 미작성).

---

## 7. 용량 추산

500MB 제약(C-04)에 대한 점검입니다.

| 테이블 | 행당 대략 | 1만 사용자 × 각 10회 가정 | 비고 |
| --- | --- | --- | --- |
| `recommendation` | ~120 B | 10만 행 ≈ 12 MB | |
| `recommendation_item` | ~120 B | 45만 행 ≈ 54 MB | 추천당 4~5행 |
| `combination_query` | ~400 B | 10만 행 ≈ 40 MB | `summary` 가 큼 |
| `simulation_run` | ~150 B | 10만 행 ≈ 15 MB | |
| `simulation_point` | ~40 B | 310만 행 ≈ 124 MB | **가장 큼** |
| `backtest_summary` | ~4 KB | 1천 행 ≈ 4 MB | JSONB 3개 |
| `doc_chunk` | ~5 KB | 178 행 ≈ 0.9 MB | 고정 |
| **합계** | | **약 250 MB** | 인덱스 제외 |

> **인덱스를 더하면 500MB 에 근접합니다.** 위 가정(1만 사용자)은 이 프로젝트 규모에서
> 비현실적으로 크지만, `simulation_point` 가 가장 먼저 문제가 된다는 사실은 유효합니다.
> **완화책**: 실행당 최대 31행 상한이 이미 있고, 필요해지면
> (ⓐ 비로그인 이력 90일 보관 후 삭제, ⓑ `simulation_point` 를 JSONB 로 접기)
> 중 ⓐ 를 먼저 씁니다. **지금 단계에서는 조치하지 않습니다.**

---

## 관련 문서

- [`ERD.md`](ERD.md) — 관계도와 설계 근거
- [`외부-데이터소스.md`](외부-데이터소스.md) — 저장하지 않는 데이터를 어디서 가져오는가
- [`../60-운영/배포-전략.md`](../60-운영/배포-전략.md) — 환경변수·키 관리 (미작성)
- [`../00-index/변경이력.md`](../00-index/변경이력.md) — CN-001 · CN-011 · CN-019 · CN-021~
