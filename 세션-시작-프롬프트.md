# 세션 시작 프롬프트 — investment-dashboard

> 새 Claude Code 세션을 `projects\investment-dashboard` 에서 열고 **이 파일 전체를 첫 메시지로 붙여넣으세요.**
> 리서치 결과가 갱신되면 이 파일도 함께 갱신합니다.

---

## 0. 함께 첨부할 것 (순서대로)

1. `AGENTS.md`
2. 이 파일

> ⚠️ **상위 정본 저장소 `quant-contract` 는 2026-09-15 제거됐다** (서브모듈 제거 +
> 호스트 저장소 삭제). `CONTEXT.md`·`research/*.md`·`docs/contracts/api-contract.md` 는
> 이제 어디에도 없다. 이 저장소 자체 문서(`AGENTS.md`·`docs/decisions/`)가 정본이다.

## 1. 지금 여는 폴더

```
C:\Users\kik32\workspace\EST-Camp-AI-Quant\projects\investment-dashboard
```

✅ 이 폴더는 **git 서브모듈이자 독립 저장소**입니다. `investment-portfolio-site` 를
개명해 승격했으므로 히스토리와 원격(origin·gitlab)이 그대로 이어집니다.

✅ 호스트 개명도 **완료**됐습니다 (2026-08-16).

| 원격 | 저장소 |
|---|---|
| `origin` | `github.com/EST-Bootcamp-Dongwon/investment-dashboard` |
| `gitlab` | `gitlab.com/dev-dongwon05253/est-investment-dashboard` |

> **왜 `dashboard` 가 아니라 `investment-dashboard` 인가.** 모노레포 루트에 이미
> `dashboards/` 서브모듈(작업 진행 대시보드)이 있어 이름이 겹쳐 혼동을 낳았다.
> **GitLab 은 경로에 `est-` 접두사를 붙이는 규칙**이라 두 호스트의 이름이 다르다.
> 그래서 `.gitmodules` 의 상대 경로는 **GitHub 기준으로만** 정확히 풀린다 —
> 자세한 내용과 GitLab clone 시 대처는 `projects/.gitmodules` 머리말 참조.

## 2. 이 모듈이 무엇인가

**①을 승격한 포트폴리오·리스크 분석 대시보드 + BI 산출물.** 신규 개발이 아니라 데이터 계층 교체.

**이 레포는 ① `investment-portfolio-site`를 개명해 승격한 것이다**(2026-08-16).
복사본이 아니라 같은 저장소이고 히스토리가 이어진다. **코드는 이미 여기 있다.**

`app/frontend/`는 이미 vanilla JS SPA이고 빌드 단계가 없다.

🔒 **목표는 이 개인 프로젝트를 완성하는 것이다.** 배포돼 있고, README에 썸네일이 있고,
클론해서 `docker compose up` 하면 끝까지 도는 상태. **9월 팀 프로젝트는 2주 × 3개 = 6주
점진 확장이고, 그때 이 완성본을 가져다 쓰는 것이지 지금의 설계 전제가 아니다.**
"팀원이 붙으면"·"융합 요건"을 근거로 무언가를 추가하지 마라 — 개인 완성도를 높이는
근거가 따로 있을 때만 한다.

⚠️ **다른 모듈로 나가는 코드는 이제 없다 (2026-09-15 갱신).** `backtest-service`·
`quant-core` 는 개발하지 않기로 해 제거됐다 — `app/src/Backtest.py` 를
`backtest-service` 로, `app/backend/indicators.py` 를 `quant-core` 로 옮기려던
계획은 폐기됐고, 둘 다 이 저장소에 남는다.

🔒 **`PortfolioOptimizer.py`·`RiskManager.py` 는 여기 남는다.** 최적화(MVO·Risk Parity)와
리스크(VaR·CVaR)는 이 레포 안에서 완성한다 — 화면 4종(`views/portfolio*.js`)과 `risk.js`,
라우터 4종이 이미 여기 있어서, 계산만 떼면 돌아가는 완성품 하나가 반쪽 둘이 된다
(ADR-DB-0003). ~~`portfolio-service`~~ 는 폐기됐다.

## 3. 절대 제약 — 이걸 어기는 제안은 하지 말 것

| # | 제약 | 근거 |
|---|---|---|
| 1 | **LLM 유료 API 비용 0원** | 강사님 방침 |
| 2 | **매매 신호 생성 경로에 LLM 금지** | 비결정성 · 재현 불가 · 사전학습 룩어헤드 오염 |
| 3 | RAG·Agent는 신호 경로와 **물리적으로 분리된 별도 서비스** | 2번의 귀결 |
| 4 | AWS 등 클라우드 인프라 미사용 | 강사님 방침 |
| 5 | Docker 이미지 용량 최소화 | 로컬 디스크 제약 |
| 6 | **CI/CD를 필수 경로에 두지 않음** | GitHub 계정 리스크 |
| 7 | **헤비 프론트 프레임워크 금지** (Next.js·React SPA·번들러) | 강사님 방침. 빌드 단계가 생기면 6번도 흔들림 |

## 4. 이미 확정된 것 — 다시 논의하지 말 것

- 🔒 **프론트 스택은 vanilla JS 유지로 확정** (ADR-DB-0001, 절대 제약 7).
  Next.js·React·TypeScript·번들러 **제안 금지**
- 서버 렌더링이 필요하면 **FastAPI + Jinja2까지**. Django 신규 도입 안 함
- 차트 라이브러리는 **CDN `<script>` 한 줄로 붙는 것만**.
  🔒 **현행 ApexCharts 3.54.0 유지가 기본**(`index.html:151`에 이미 있음).
  교체에는 근거가 필요하고 유지에는 필요 없다. Lightweight Charts 는 **캔들 성능
  실측에서 막힐 때만 추가**(교체 아님) — 05 검증본 §5.4
- **세 갈래 분리**: 제품 화면(`app/`) / 정적 리포트(`reports/figures/`) / BI 도구(`bi/`)
- **BI 도구는 아키텍처 밖.** CSV 파일 한 장으로만 연결
- ①의 Dockerfile에서 **torch·diffusers·opencv 제거**.
  ⚠️ 단 현행 Dockerfile은 이미 **CPU 전용 휠**(`download.pytorch.org/whl/cpu`)을 쓴다 —
  절감액이 "700MB~1.5GB" 추정보다 작다. 실측 전에는 총량을 근거로 쓰지 말 것
  ⚠️ **`fonts-nanum` 설치 줄과 `CMD ["uvicorn","app.backend.main:app",…]` 는 건드리지 말 것.**
  폰트를 빼면 `backend/charting.py` 의 한글 라벨이 □ 로 나오고, 엔트리포인트는 이 값이 정본이다
- Vercel: Fluid compute **전 플랜 300초**, 요청·응답 본문 **4.5MB 한도**
- 🔒 **API 계약은 이제 이 저장소 안에서 관리한다.** 정본이던
  `quant-contract/docs/contracts/api-contract.md` 는 2026-09-15 저장소 제거로
  사라졌다 — 남은 규약은 `app/frontend/js/api.js` 주석과
  `docs/decisions/0001-vanilla-js-and-no-typescript.md` 에 있다. 에러 본문의
  `detail` 은 반드시 문자열
- 🔒 **포트폴리오 최적화·리스크는 이 레포가 최종 자리다**(ADR-DB-0003). 분리 제안 금지
- **`skfolio`(BSD-3)는 교차검증 기준선이고 ①의 구현이 정본이다**(ADR-DB-0004).
  대체가 아니다 — 같은 입력으로 `MeanRisk`·`HierarchicalRiskParity` 를 돌려 대조만 한다

## 5. legacy — 읽기 전용

### 승격된 것 — 더 이상 legacy 가 아니다 (2026-08-16)

이 둘은 `upstream`(강사님 원본) 원격이 없어 3-way merge 계보 부담이 없다.
그래서 **복사하지 않고 폴더를 개명해 그 자리에서 발전시킨다.** 히스토리가 이어진다.

| 기호 | 이전 이름 | 현재 |
|---|---|---|
| ④ | `api-test` | **`projects/data-service`** |
| ① | `investment-portfolio-site` | **`projects/investment-dashboard`** |

### 읽기 전용 — 수정 금지

코드를 옮길 때는 이 모듈 안에 **복사한 뒤** 수정한다.

| 기호 | 경로 | 스택 | 계보 |
|---|---|---|---|
| ② | `C:\Users\kik32\workspace\Research-Prompt-Engineering` | Python | **projects 밖!** |
| ③ | `projects/stock-coin-trade` | Django 5.2.17 + DRF 3.18 + HTMX/Alpine | `upstream` + `upstream-main` |
| ⑤ | `projects/docker-class` | Docker·DevSecOps 실습 | `upstream` + `upstream-main` |
| — | `projects/investment-analysis` | 강의 원본 | `upstream` |

**③⑤와 investment-analysis 는 폴더를 이동·개명하지 않는다.** 강사님 원본과 3-way merge
계보가 살아 있어서(`upstream-main` 브랜치), 파일을 대거 이동한 뒤 upstream 을 얹으면
병합이 깨진다. ③은 추가로 **체결 리팩터링의 회귀 확인 대상**이라 계속 돌아가야 한다.

각 레포의 `NOTICE.md`(원저작자 edumgt 표시)는 승격 후에도 유지한다.

## 6. 이번 세션의 목표

> 순서를 바꾸지 마세요. **작업 1이 나머지 전부의 선행**입니다.

**진척 (2026-08-17 기준).** 아래 표가 정본이다 — 끝난 것을 다시 하지 마라.

| 작업 | 상태 |
|---|---|
| 1 fetch 회수 | ✅ 완료 (`check_layers.py` 규칙 7번이 강제) |
| 2 절대 제약 1 위반 제거 | ✅ 완료 |
| 3 ADR-DB-0001·0002 | ✅ 완료 |
| 4 승격 정리 | ✅ 완료 |
| 5 Dockerfile 경량화 | ✅ 완료 (2.49GB → 1.04GB) |
| 6 OpenAPI 스펙 파일 | ☐ 미완 |
| 7 백테스트 결과 화면 | 🔶 F08·F07·F14 이전 완료. **서버 렌더 차트 7곳 남음** (명세: `docs/승격/차트-이전-명세.md`) |
| 8 `bi/` 대시보드 | ☐ 미완 |
| 9 포트폴리오·리스크 완성 | ☐ 미완 (2026-08-17 신설) |

**작업 1 (3~4h). fetch 회수 — 이번 세션의 본체.**
`js/api.js` 밖에서 `fetch()` 를 부르는 곳이 **7개 파일 10건**입니다(05 검증본 §부록 B 실측).
이 상태로는 API 계약이 확정돼도 강제되지 않습니다.
- 치환 순서는 05 검증본 **§5.2 표 그대로** (`includeSidebar` → `youtube` → `taxAccounting`
  → `adminRagIndex` → `home` → `ragChat` → `backtest-lab`)
- `const BASE = ''` 를 **`<meta name="api-base">` 주입**으로 승격 (api-contract §8.2).
  환경변수 치환·번들러를 쓰지 않습니다 — 빌드 단계가 생기면 절대 제약 7 위반입니다
- `apiFetch` 에 `e.code`·`e.status`·`e.traceId` 추가. **`new Error(err.detail)` 은 그대로**
- 회귀 확인은 **기존 `scripts/verify_*.py` 18개**로. 새 테스트를 쓰지 않습니다
- 끝나면 `scripts/check_layers.py` 에 "api.js 외 파일의 `fetch(` 금지" 검사 3줄 추가

**작업 2 (30m). 절대 제약 1 위반 제거.**
`docker-compose.yml` 의 `RAG_LLM_BASE_URL`(기본값이 `https://api.openai.com/v1`) ·
`RAG_LLM_API_KEY` · `RAG_LLM_MODEL` **3줄 삭제**. 파일(`clients/rag_llm.py`)만 지우면
안 됩니다 — **compose·env·라우터·파일 4곳이 동시에** 끝나야 완료입니다.
같이: `.gitignore` 에 `!/data/exports/` 예외 (지금 `/data/` 에 걸려 클론 시 폴더가 없음).

**작업 3 (1h). ADR-DB-0001 · 0002 작성** — `docs/decisions/`, MADR-minimal.
전 모듈 ADR 실물이 아직 0건입니다. 이게 첫 번째입니다.

**작업 4 (1h). 승격 정리** — 루트 `README.md`를 `docs/승격/module-readme-초안.md`로 교체,
`app/src/OpenCVCPU.py` · `app/backend/services/rag.py` 폐기.
⚠️ `Backtest.py` 와 `indicators.py` 만 다른 모듈로 가므로 **지금 지우지 않습니다.**
`PortfolioOptimizer.py`·`RiskManager.py` 는 **여기 남습니다**(작업 9).

**작업 5 (2h). Dockerfile 경량화** — `.dockerignore` 신설(`lean-cli/` 226MB 가 빌드
컨텍스트에 들어가고 있음) + `USER appuser`. **교체가 아니라 최소 diff**(05 검증본 §5.5).

**작업 6 (1h). OpenAPI 스펙을 파일로 떨군다** — `openapi_docs.py` 는 런타임
메타데이터이고 `openapi.json` 은 레포에 없습니다(05 검증본 P0-4). 파일로 있어야
강사님 요건 1번(Swagger HTML)이 산출물로 남고, 응답이 조용히 바뀌면 diff 로 보입니다.
❌ **목서버(Prism)는 하지 않습니다.** 남의 백엔드가 늦을 때를 위한 장치라 지금은
컨테이너 하나와 스펙 동기화 부담만 늘립니다. 필요해지면 그때 한 줄이면 됩니다.

**작업 7 (6h).** 백테스트 결과 화면 (에쿼티 커브 · 드로다운 · `run_id` 다중 비교).
**작업 1을 건너뛰고 오면 이게 11번째 fetch 가 됩니다.**

**작업 8 (4h).** `bi/` — CSV 익스포트를 읽는 BI 대시보드 1장 + 스크린샷.
**README 썸네일(강사님 요건 4번)을 겸하므로 개인 완성도에 직접 기여합니다.**

**작업 9 (4h). 포트폴리오·리스크를 이 레포 안에서 완성한다**(ADR-DB-0003).
- `app/src/PortfolioOptimizer.py`·`RiskManager.py` 의 독립 스크립트 부분(`__main__`·`savefig`)을 걷어낸다
- 기존 라우터(`quant.py`·`simulation.py`·`combination.py`)와의 중복을 정리한다 —
  **같은 계산이 두 곳에 있으면 반드시 갈라진다**
- 응답은 **JSON(수치).** 이미지 파일을 만들지 않는다
- `skfolio` 교차검증 결과를 `docs/explanation/cross-validation-with-skfolio.md` 에
  남긴다 — **일치든 불일치든** 남긴다(ADR-DB-0004)

## 7. 완료 조건 (DoD)

- [ ] **`js/api.js` 밖에서 `fetch(` 를 부르는 곳이 0건이다** (`check_layers.py` 가 강제한다)
- [ ] `<meta name="api-base">` 한 줄만 고치면 백엔드 주소가 바뀐다
- [ ] **compose·env·라우터·파일 어디에도 유료 LLM 호출 경로가 없다**
- [ ] `npm run build` 같은 빌드 단계가 **없다**
- [ ] `openapi.json` 이 레포에 파일로 있다 (❌ 목서버는 하지 않는다 — 작업 6)
- [ ] Dockerfile에 torch·diffusers·opencv가 **없고** 용량이 실측돼 있다
      (한글 폰트와 엔트리포인트는 그대로다)
- [ ] Vercel에 배포돼 있고 GitHub Actions를 거치지 않는다 (Git 웹훅만)
- [ ] `bi/screenshots/`에 대시보드 이미지가 있고 README 썸네일을 겸한다 (강사님 요건 4번)
- [ ] **ADR-DB-0001 ~ 0004 가 `docs/decisions/`에 있다**
- [ ] **문서에 `portfolio-service` 언급이 0건이다**
- [ ] **문서에 "2주짜리 팀 프로젝트"가 남아 있지 않다**
- [ ] **최적화·리스크 API 가 `/docs` 에 있고 JSON(수치)으로 응답한다**
- [ ] **skfolio 교차검증 결과가 문서에 있다** (일치든 불일치든)

## 8. 첫 지시

**작업 1부터 바로 시작해줘.** `api.js` 가 단일 진입점인지는 **이미 조사가 끝났다** —
아니고, 아래 7개 파일 10건이 직접 `fetch()` 를 부른다(2026-08-16 실측). 다시 조사하지 마라.

| 파일 | 건수 |
|---|---:|
| `js/views/ragChat.js` | 2 |
| `js/views/home.js` | 2 |
| `js/pages/backtest-lab.js` | 2 |
| `js/views/taxAccounting.js` | 1 |
| `js/views/adminRagIndex.js` | 1 |
| `js/pages/youtube.js` | 1 |
| `js/includeSidebar.js` | 1 |

먼저 이 10곳을 각각 열어 **무엇을 부르고 무엇을 기대하는지** 표로 정리한 뒤,
`apiFetch` 로 흡수할 때 시그니처 확장이 필요한 것(파일 업로드·토큰 헤더·스트리밍)을
분류해줘. 그다음 §6 작업 1의 순서대로 치환한다.

⚠️ 이 레포는 서브모듈이지만 **읽기 전용이 아니다.** 여기가 작업 대상이다.
⚠️ `app/src/Backtest.py` 와 `app/backend/indicators.py` **둘만** 다른 모듈로 갈 것이니
   지금 지우지 마라. **포트폴리오·리스크는 여기가 최종 자리다**(ADR-DB-0003).
⚠️ **포트폴리오 기능을 다른 서비스로 빼자는 제안을 하지 마라.** 2026-08-17 에 정정한 실수다.
⚠️ 프레임워크 전환은 확정적으로 하지 않는다. 그 방향의 제안은 하지 마라.
⚠️ **차트 라이브러리 교체를 제안하지 마라.** ApexCharts 유지가 확정이고, 예외는
   캔들 성능 실측에서 막힐 때뿐이다(§4).

---

## 부록. 이 프로젝트의 컨벤션

- Python 3.12 / ruff (line-length 100) / 들여쓰기 2 spaces(설정 파일)·4 spaces(파이썬)
- 의존성 정본은 `pyproject.toml` + `uv.lock`. **`requirements.txt`는 `uv export` 생성물이므로 직접 편집 금지**
- 검증 정본은 `invoke check` 하나. Makefile·tox·check.py 를 따로 두지 않는다
- ADR은 `docs/decisions/NNNN-title.md`, 참조 키는 `ADR-{스코프}-{번호}`
- 다이어그램은 Mermaid `flowchart` + `subgraph`. **`C4Context`/`C4Container` 문법 금지**(GitHub 미렌더링)
- 커밋 메시지: `<scope>: <동사원형 요약>` (예: `api: add run_id filter to backtest endpoint`)
- 문서 문체: 개조식("-다"). 튜토리얼만 "-습니다"
- 모든 대화·주석·문서는 **한국어**. 변수·함수명만 영어
